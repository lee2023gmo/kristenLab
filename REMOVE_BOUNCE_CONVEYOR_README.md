# 移除反弹块和传送带

## 本次修改目标

不再使用：

```text
反弹块 5
传送带 7
```

保留：

```text
空地 0
墙体 1
起点 2
终点 3
死亡区 4
缓冲区 6
数据碎片 8
四方向蹦床 9/u/v/w
候选点 E
```

## 修改内容

### 1. 关卡设计师

元素下拉框中已移除：

```text
弹射块 B
传送带 →
```

现在不能再从设计师中新建这两种元素。

### 2. 玩家编辑模式

游戏底部按钮中已移除：

```text
弹射块
传送带
```

玩家编辑候选点时，现在可选择：

```text
缓冲区
蹦床↗
蹦床↖
蹦床↘
蹦床↙
```

右键候选点仍然可以清空为空地。

### 3. 候选点限制

候选点不能再标记在旧的反弹块 / 传送带上。

### 4. 兼容说明

为了避免旧 JSON 直接崩掉，代码里仍保留对 `5` 和 `7` 的识别与显示逻辑。

也就是说：

```text
旧地图里如果已经有 5 或 7，仍然能被读取
但新设计和玩家编辑都不会再提供这两个元素
```

## 修改文件

- `leveleditordialog.cpp`
- `gamescene.h`
- `gamescene.cpp`
- `mainwindow.cpp`

## 验收方法

### 验收 1：设计师下拉框移除

1. 打开 `关卡设计师`。
2. 展开左侧 `元素` 下拉框。
3. 不应再看到：
   - `弹射块 B`
   - `传送带 →`
4. 应还能看到：
   - `缓冲区`
   - `候选点 E`
   - `蹦床 ↗/↖/↘/↙`

### 验收 2：玩家编辑按钮移除

1. 进入一张带候选点的关卡。
2. 点击 `进入编辑模式`。
3. 底部按钮不应再有：
   - `弹射块`
   - `传送带`
4. 应还能看到：
   - `缓冲区`
   - `蹦床↗`
   - `蹦床↖`
   - `蹦床↘`
   - `蹦床↙`

### 验收 3：候选点直接编辑正常

1. 点击 `缓冲区`。
2. 点击候选点。
3. 候选点应直接变成缓冲区。
4. 点击 `蹦床↗/↖/↘/↙`。
5. 点击候选点。
6. 候选点应直接变成对应方向蹦床。
7. 右键候选点，应变回空地。

### 验收 4：新保存 JSON 不再出现 5/7

1. 新建一张地图。
2. 只使用当前下拉框中的元素。
3. 保存 JSON。
4. 打开 JSON 检查 map 字符串。
5. 正常情况下不应再出现 `5` 或 `7`。

### 验收 5：旧地图兼容

1. 导入一张旧地图，如果里面有 `5` 或 `7`。
2. 程序不应崩溃。
3. 旧元素可以显示，但不能从下拉框或玩家编辑按钮中新建。
