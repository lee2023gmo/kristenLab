# 大地图缩放、滚动条、目标上限 999 修正版

## 修改内容

### 1. 目标反转次数上限提升到 999

原来：

```text
0 ~ 99
```

现在：

```text
0 ~ 999
```

### 2. 缩放比例进一步放宽

原来最小单元格：

```text
20 px
```

现在最小单元格：

```text
6 px
```

最大单元格：

```text
60 px
```

每次放大 / 缩小：

```text
2 px
```

### 3. 大地图自动缩得更小

自动缩放规则现在大致为：

```text
140×140 及以上：6 px
120×120 及以上：8 px
80×80 及以上：10 px
60×60 及以上：12 px
45×45 及以上：16 px
35×30 及以上：20 px
```

### 4. 表格增加横向和纵向滚动条

地图表格现在强制显示：

```text
横向滚动条
纵向滚动条
```

这样可以浏览 150×150 地图的四个边角。

### 5. 不再让地图表格撑爆外框

`adjustEditorSizeToMap()` 改为：

```text
表格视口保持合理大小
地图内容超出后使用滚动条查看
```

而不是让表格本身变得比窗口还大。

## 修改文件

- `leveleditordialog.cpp`

## 验收方法

1. 打开 `关卡设计师`。
2. 将目标反转次数调到 `999`，应可以成功。
3. 设置宽度 `80`，高度 `80`，点击 `生成地图`。
4. 地图应自动缩到约 `10 px / 24%`。
5. 设置宽度 `150`，高度 `150`，点击 `生成地图`。
6. 地图应自动缩到约 `6 px / 14%`。
7. 右侧地图区域应出现横向和纵向滚动条。
8. 拖动横向滚动条到最右边，应能看到最右侧地图边缘。
9. 拖动纵向滚动条到最下方，应能看到最下侧地图边缘。
10. 点击 `放大地图`，格子应逐渐变大。
11. 点击 `缩小地图`，格子应逐渐变小。
12. 点击 `还原缩放`，格子应恢复到 `42 px / 100%`。
13. 缩放和滚动后，左键绘制、右键擦除、保存 JSON 应仍然正常。
