# 蹦床弹射撞侧墙后继续按重力坠落修正

## 问题

截图中的情况是：

```text
角色从下方蹦床斜向弹出
撞到右侧墙
此时当前重力方向是 ↑
按规则应该继续向上坠落
但由于方形碰撞体贴着右墙，竖直移动也被判定为撞墙
所以角色卡死在墙边
```

## 本次修改

修改文件：

```text
gamescene.h
gamescene.cpp
```

核心变化：

```text
1. 斜向蹦床撞到左/右侧墙时，不再把 y 速度清零
2. 如果当前重力是 ↑，撞侧墙后继续向上坠落
3. 如果当前重力是 ↓，撞侧墙后继续向下坠落
4. 纯竖直坠落时，允许忽略已经贴着的左/右侧墙
5. 真正向左/向右移动时，左右墙仍然严格检测，不能穿墙
6. 竖直撞到上/下墙时仍然停止
```

## 验收标准

### 验收 1：截图位置不再卡死

1. 进入你截图里的 `弹簧练习` 关卡。
2. 用右下角的 `蹦床↗` 把角色弹向右墙。
3. 角色撞到右墙后，不应卡在原地。
4. 当前重力是 `↑` 时，角色应继续向上坠落。
5. 顶部状态可以仍显示 `空中不可切换`，这是正常的。

### 验收 2：左墙同理

1. 放置或选择一个会把角色弹向左墙的斜向蹦床，例如 `蹦床↖`。
2. 让角色撞到左墙。
3. 如果当前重力是 `↑` 或 `↓`，角色应继续沿当前重力方向坠落。
4. 不应卡在左墙边。

### 验收 3：真正撞到天花板仍然停止

1. 让角色在重力 `↑` 时继续向上坠落。
2. 撞到上方墙体后应停住。
3. 不应穿进天花板。

### 验收 4：真正撞到地面仍然停止

1. 让角色在重力 `↓` 时向下坠落。
2. 撞到下方墙体后应停住。
3. 不应穿进地面。

### 验收 5：不能横向穿墙

1. 让角色贴着地面或天花板向右走到竖直墙前。
2. 角色应停在墙前。
3. 不应穿进右墙。
4. 左墙同理。

### 验收 6：水平蹦床仍然碰墙停下

1. 使用 `蹦床→` 或 `蹦床←`。
2. 让角色水平弹向一面墙。
3. 角色应碰墙后停下。
4. 不应穿墙。
