# 关卡设计师大地图性能优化

## 修正原因

150×150 地图有 22500 个格子。

之前慢的主要原因是：

```text
setCellTile() 每改一个格子都会 updateMapPreview()
updateMapPreview() 又会重新读取整张表格并拼接地图字符串
```

生成地图、清空地图时会变成：

```text
22500 次格子更新 × 22500 格子扫描
```

这会非常慢。

## 本次优化

修改文件：

- `leveleditordialog.h`
- `leveleditordialog.cpp`

### 1. 新增批量更新模式

新增成员：

```cpp
bool isBulkUpdating;
```

生成地图、导入地图、清空地图、自动边框墙时：

```text
先进入批量模式
中间不刷新预览
最后只刷新一次
```

### 2. 禁用批量过程中的表格重绘

批量操作时使用：

```cpp
mapTable->setUpdatesEnabled(false);
QSignalBlocker blocker(mapTable);
```

操作完成后再恢复。

### 3. 大地图预览简化

超过 5000 个格子的地图，不再实时显示完整地图字符串。

例如 150×150 会显示：

```text
大地图预览已简化，以提升编辑性能。
地图大小：150 行 × 150 列，共 22500 个格子。
保存时仍会完整写入 JSON。
```

保存 JSON 时仍然会完整读取全部地图，不影响保存结果。

### 4. 缩放刷新也优化

缩放时也会暂时关闭表格重绘，避免大地图缩放时频繁刷新。

## 验收方法

1. 打开 `关卡设计师`。
2. 设置宽度 `80`，高度 `80`。
3. 点击 `生成地图`，速度应明显快于之前。
4. 点击 `清空`，速度应明显快于之前。
5. 设置宽度 `150`，高度 `150`。
6. 点击 `生成地图`，应能在可接受时间内完成。
7. 地图字符串预览应显示简化提示，而不是完整 150 行字符串。
8. 点击 `清空`，不应像之前一样长时间卡住。
9. 点击 `边框墙`，应比之前明显更快。
10. 放置起点和终点后保存 JSON，保存结果应仍然完整。
