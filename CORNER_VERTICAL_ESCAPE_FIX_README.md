# 内凹角 y 方向脱困修正版

## 为什么之前还会卡死

前几版主要处理的是：

```text
横向滚动时，x 方向被平台边缘挡住
```

也就是：

```text
velocity.x != 0 时被挡
```

但是你这次截图里的情况不完全一样。

这次小球已经从“横向滚动”切换成了：

```text
按当前重力方向向上坠落
```

也就是：

```text
velocity.y != 0
```

然后第一步向上时，球的顶部碰到了平台角落的侧边，于是旧逻辑直接：

```cpp
velocity.setY(0);
```

所以就卡在了内凹角。

## 本次修改

修改文件：

- `gamescene.cpp`

核心修正：

当小球已经没有真正的上/下支撑，但 y 方向坠落被角落挡住时：

1. 不再直接把 `velocity.y` 设成 0。
2. 会向左和向右各试探一小段距离。
3. 如果找到一个可以继续向上/向下坠落的位置，就把球小幅挪出角落。
4. 然后继续按当前重力方向坠落。

同时把横向探出距离从：

```text
半个格子
```

放宽到：

```text
3/4 个格子
```

这样对内凹角更稳，但不会瞬移太远。

## 验收方法

### 验收 1：你截图里的这个角

1. 使用你当前截图里的地图。
2. 让小球重力向上。
3. 让小球贴着上方结构移动到这个内凹角。
4. 到这个角时，小球不应该卡死。
5. 它应该小幅向左或向右脱出角落，然后继续向上坠落。

### 验收 2：上方墙从左往右

1. 让小球重力向上。
2. 贴着上方墙从左往右走到平台边缘。
3. 不应该卡死。
4. 应该离开支撑后向上掉。

### 验收 3：上方墙从右往左

1. 让小球重力向上。
2. 贴着上方墙从右往左走到平台边缘。
3. 不应该卡死。
4. 应该离开支撑后向上掉。

### 验收 4：下方平台从左往右

1. 让小球重力向下。
2. 沿着下方平台从左往右滚出边缘。
3. 不应该卡死。
4. 应该向下掉。

### 验收 5：下方平台从右往左

1. 让小球重力向下。
2. 沿着下方平台从右往左滚出边缘。
3. 不应该卡死。
4. 应该向下掉。

### 验收 6：普通撞墙仍然正常

1. 让小球正面撞墙。
2. 小球应该停住。
3. 不应该反弹。
4. 不应该穿墙。
