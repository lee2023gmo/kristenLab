# 方形碰撞体贴墙走路卡住修正

## 问题

队友为了防止人物穿模，把角色碰撞体改成了更接近方形的区域。

合并后出现一个问题：

```text
角色贴着地面 / 天花板时，
某些位置明明右侧是空地，
但按右键无法向右走。
```

原因是严格 AABB 方形碰撞会把“正在贴着的支撑墙”也当成阻挡。

例如角色站在地面上时，碰撞体下边缘和地面墙块非常接近，横向移动检测会误判为撞墙，于是阻止向右移动。

## 本次修改

修改文件：

```text
gamescene.h
gamescene.cpp
```

新增逻辑：

```text
1. 保留方形碰撞体思路
2. 碰撞半边长从完整 12 改为检测用 10，留 2px 安全皮肤
3. 横向移动时，如果角色正贴着下方墙，允许继续接触下方支撑墙
4. 横向移动时，如果角色正贴着上方墙，允许继续接触上方支撑墙
5. 左右墙仍然严格检测，防止横向穿墙
6. 竖直移动仍然严格检测，防止上下穿墙
```

新增函数：

```cpp
canBallMoveToForVelocity()
canBallMoveToWithSupportAllowance()
collisionRadius()
```

## 验收标准

### 验收 1：截图位置可以向右走

1. 进入你截图里的那一关和位置。
2. 角色站在地面上。
3. 按右方向键。
4. 角色应该可以向右移动。
5. 不应再原地卡住。

### 验收 2：向左也正常

1. 在同一类地面位置。
2. 按左方向键。
3. 角色应可以向左移动。
4. 不应因为贴着地面而卡住。

### 验收 3：贴天花板也能横向走

1. 让角色重力向上，贴着上方墙。
2. 按左 / 右方向键。
3. 角色应能沿着天花板横向移动。
4. 不应因为贴着上方墙而被卡死。

### 验收 4：不能穿左右墙

1. 让角色贴着地面向右走到一面竖直墙前。
2. 角色应停在墙前。
3. 不应穿进右侧墙。
4. 向左墙测试同理。

### 验收 5：不能上下穿墙

1. 让角色向上撞天花板。
2. 应停住，不应穿进墙。
3. 让角色向下撞地面。
4. 应停住，不应穿进墙。

### 验收 6：离开平台仍会坠落

1. 让角色沿地面向右走到平台边缘。
2. 离开支撑后，应按当前重力方向坠落。
3. 不应悬空继续横向吸附。
