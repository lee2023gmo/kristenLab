# 新增道具：蹦床

## 本次实现目标

新增一个特殊功能块：

```text
9：蹦床
显示：T
```

蹦床用于让竖直下落的小球被垂直弹出去。

## 修改文件

- `tiledefs.h`
- `gamescene.h`
- `gamescene.cpp`
- `leveleditordialog.cpp`
- `mainwindow.cpp`

## 规则说明

### 1. 竖直下落触发

当小球竖直向下进入蹦床格子 `9` 时：

```text
向下速度 -> 向上速度
重力显示 -> ↑
```

也就是小球会被蹦床垂直向上弹出去。

### 2. 对称处理

当小球竖直向上进入蹦床格子 `9` 时：

```text
向上速度 -> 向下速度
重力显示 -> ↓
```

这样上下方向都能演示。

### 3. 水平经过不触发

如果小球水平经过蹦床：

```text
不会触发弹跳
```

因为这次需求是“竖直下落的球”被蹦床弹出去。

### 4. 空中不能改变方向

蹦床把小球弹到空中后，仍然遵守现有规则：

```text
四面没有上/下支撑时，不能改变方向
```

所以玩家不能在空中乱改方向。

### 5. 碰墙停下

小球被蹦床弹出去后，如果碰到墙：

```text
不反弹
直接停下
```

仍然遵守之前的撞墙停止规则。

## 关卡设计师

关卡设计师下拉框新增：

```text
蹦床 T
```

保存 JSON 时使用字符：

```text
9
```

## 游戏内编辑模式

游戏底部按钮新增：

```text
蹦床
```

候选编辑点循环也改成：

```text
空地 -> 弹射块 -> 缓冲区 -> 传送带 -> 蹦床 -> 空地
```

## 验收方法

### 验收 1：设计师能放置蹦床

1. 打开 `关卡设计师`。
2. 点击 `生成地图` 或导入已有地图。
3. 在元素下拉框选择 `蹦床 T`。
4. 点击格子放置。
5. 表格中应显示 `T`。
6. 底部地图字符串中对应位置应保存为 `9`。
7. 保存后 JSON 中应出现字符 `9`。

### 验收 2：竖直下落触发蹦床

1. 做一个简单地图。
2. 在小球下方放置一个蹦床 `9`。
3. 让小球重力向下，竖直落到蹦床上。
4. 小球碰到蹦床后应向上弹出。
5. 顶部状态栏重力应显示 `↑`。

### 验收 3：空中不能改变方向

1. 小球被蹦床弹到空中后，立刻按左/右/下方向键。
2. 如果小球没有贴着上方或下方墙，方向不应改变。
3. 状态栏应仍显示类似 `空中不可切换`。

### 验收 4：碰墙停下

1. 在蹦床上方放一堵墙。
2. 小球向下落到蹦床后会向上弹。
3. 碰到上方墙后应停住。
4. 不应该反弹回来。

### 验收 5：水平经过不触发

1. 让小球沿平台水平经过蹦床。
2. 如果小球没有竖直进入蹦床，不应被弹起。
3. 只有竖直方向进入蹦床才触发弹跳。
