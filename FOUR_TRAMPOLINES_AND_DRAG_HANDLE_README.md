# 四方向蹦床 + 底部拖动窗口修正版

## 本次修正 1：窗口顶部出屏后难以拖动

新增底部拖动条：

```text
⇕ 标题栏被屏幕挡住时，可按住这里或窗口底部空白处拖动窗口
```

同时增加窗口位置保护：

- 打开窗口时会自动移动到屏幕可见范围内；
- 地图生成 / 缩放导致窗口尺寸变化后，也会自动保持在屏幕内；
- 按住窗口底部 36px 区域也可以拖动窗口。

## 本次修正 2：四方向蹦床

| 显示 | JSON 字符 | 弹出方向 |
|---|---|---|
| ↗ | `9` | 45° 右上 |
| ↖ | `u` | 45° 左上 |
| ↘ | `v` | 45° 右下 |
| ↙ | `w` | 45° 左下 |

`9` 保持兼容旧版本，仍然表示右上蹦床。

## 验收方法

### 1. 窗口底部可拖动

1. 打开 `关卡设计师`。
2. 按住底部拖动条拖动。
3. 窗口应能移动。
4. 按住窗口底部空白区域拖动，也应能移动。
5. 生成大地图或缩放后，窗口不应再跑到屏幕外导致无法拖动。

### 2. 设计师能放置四种蹦床

1. 打开 `关卡设计师`。
2. 生成地图。
3. 元素下拉框依次选择 `蹦床 ↗ / ↖ / ↘ / ↙`。
4. 点击地图格子。
5. 格子应显示对应箭头。

### 3. 保存 JSON 字符正确

1. 放置四种蹦床并保存。
2. 打开 JSON。
3. 应看到：
   - ↗ 对应 `9`
   - ↖ 对应 `u`
   - ↘ 对应 `v`
   - ↙ 对应 `w`

### 4. 进入游戏后箭头显示正确

1. 从关卡选择进入刚保存的关卡。
2. 四个蹦床应显示不同箭头。
3. 箭头方向应和设计师一致。

### 5. 四方向弹出效果

分别测试：

1. 小球竖直进入 `↗`，应向右上弹。
2. 小球竖直进入 `↖`，应向左上弹。
3. 小球竖直进入 `↘`，应向右下弹。
4. 小球竖直进入 `↙`，应向左下弹。

### 6. 斜向弹出后的限制

1. 小球被蹦床斜向弹出后，在空中按方向键。
2. 如果没有贴着上方或下方墙，不应改变方向。
3. 在弹出方向前方放墙。
4. 小球碰墙后应直接停下，不应反弹或滑墙。

### 7. 玩家编辑候选点兼容

1. 在候选点模式中，把四种蹦床所在格子标记为候选点。
2. 保存并进入游戏。
3. 进入玩家编辑模式。
4. 候选点应仍然可编辑。
5. 玩家编辑循环应能切换到四种蹦床。
