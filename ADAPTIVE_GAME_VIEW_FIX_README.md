# 游戏界面尺寸自适应修复

本次修复解决科幻终端背板与关卡地图大小脱节的问题。

## 修复内容

- 终端背板不再使用固定宽高，而是根据地图当前在视口中的实际尺寸自动包裹。
- 地图、终端背板与窗口使用同一套动态安全区域，窗口最大化、还原或拖动尺寸时会自动重新适配。
- 首次进入游戏、切换/重开关卡、点击“适应窗口”时，会根据当前窗口和地图比例自动缩放。
- 连续 Resize 事件会合并处理，降低缩放抖动。
- 无需滚动时自动隐藏滚动条；手动放大或超大地图超出视口时滚动条自动出现。
- 小地图允许适度放大，最大自动倍率为 200%。
- 场景内黄色状态文字在窄地图中会自动缩小，不再伸出终端边框。

## 手动缩放行为

点击“放大视图”“缩小视图”或“还原视图”后会暂时退出自动适配；点击“适应窗口”可重新开启。

## 修改文件

- `mainwindow.cpp`
- `mainwindow.h`
- `gamescene.cpp`

压缩包按要求不包含 `resources/` 文件夹，请覆盖到原项目中使用。
- 通过键盘切换到不同尺寸的关卡时，也会自动重新适配。
