# 设计师窗口适应屏幕 + 玩家编辑模式直接放置

## 本次修改 1：设计师模式直接适应窗口

关卡设计师打开后会自动适应显示器可用区域：

- 避开任务栏；
- 保留少量边距；
- 不再出现窗口顶部跑出屏幕导致不好拖动的问题；
- 保留右下角缩放手柄；
- 原来的底部拖动条仍然保留。

## 本次修改 2：玩家编辑模式改成“按钮选择什么，就放什么”

原来玩家编辑模式里需要反复点击候选点循环切换，现在改为：

```text
先点底部按钮选择元素
再点击候选点
候选点直接变成所选元素
```

右键候选点仍然是清空为空地。

## 本次修改 3：游戏内底部按钮支持四方向蹦床

底部按钮新增四个蹦床按钮：

```text
蹦床↗
蹦床↖
蹦床↘
蹦床↙
```

点击哪个按钮，候选点就会直接变成对应方向的蹦床。

## 修改文件

- `gamescene.h`
- `gamescene.cpp`
- `mainwindow.cpp`
- `leveleditordialog.h`
- `leveleditordialog.cpp`

## 验收方法

### 验收 1：设计师窗口适应屏幕

1. 打开 `关卡设计师`。
2. 窗口应自动适应当前显示器可用区域。
3. 顶部标题栏不应跑出屏幕。
4. 任务栏不应被覆盖。
5. 底部拖动条仍然可以拖动窗口。
6. 生成 80×80 或 150×150 地图后，窗口仍应保持在屏幕可见范围内。

### 验收 2：玩家编辑模式不再循环切换

1. 进入一张带候选点的关卡。
2. 点击 `进入编辑模式`。
3. 点击底部 `弹射块`。
4. 点击一个候选点。
5. 该候选点应直接变成弹射块。
6. 再点击同一个候选点，不应继续轮到缓冲区或传送带，仍应是弹射块。

### 验收 3：底部按钮直接放指定元素

1. 点击 `缓冲区`，再点候选点，应直接变成缓冲区。
2. 点击 `传送带`，再点候选点，应直接变成传送带。
3. 点击 `蹦床↗`，再点候选点，应直接变成 ↗ 蹦床。
4. 点击 `蹦床↖`，再点候选点，应直接变成 ↖ 蹦床。
5. 点击 `蹦床↘`，再点候选点，应直接变成 ↘ 蹦床。
6. 点击 `蹦床↙`，再点候选点，应直接变成 ↙ 蹦床。

### 验收 4：右键清空仍然正常

1. 进入玩家编辑模式。
2. 在候选点上右键。
3. 该候选点应变为空地。
4. 非候选点仍然不能被编辑。

### 验收 5：状态栏显示当前选择

1. 进入玩家编辑模式。
2. 点击不同的底部元素按钮。
3. 顶部状态栏应显示类似：
   - `状态：候选点编辑：弹射块`
   - `状态：候选点编辑：蹦床↗`
   - `状态：候选点编辑：蹦床↙`
