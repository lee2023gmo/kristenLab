# 关卡设计师：测试当前关卡合并修复

本版本在 `level_keydoor_undo_layout_merged` 的基础上补回“测试当前关卡”功能，并继续保留：

- 钥匙 `K` 与门 `A`
- 关卡设计师撤销 / 重做
- `Ctrl + Z` 撤销
- `Ctrl + Y` / `Ctrl + Shift + Z` 重做
- 左侧工具栏 `QScrollArea` 滚动适配
- 输入框、下拉框、按钮最小高度修复

## 新增/恢复功能

关卡设计师左侧按钮区新增：

```text
测试当前关卡
```

点击后流程：

1. 校验当前地图。
2. 如果地图不合法，弹窗显示失败原因。
3. 如果地图合法，直接关闭设计师窗口并进入游戏界面试玩。
4. 此测试不会保存 JSON 文件。
5. 游戏状态栏中显示 `[测试] 关卡名`。
6. 测试关卡中“上一关 / 下一关”会提示当前是临时测试关卡。

## 涉及文件

```text
leveleditordialog.h
leveleditordialog.cpp
mainwindow.h
mainwindow.cpp
gamescene.h
gamescene.cpp
DESIGNER_TEST_LEVEL_MERGE_README.md
```

## 校验规则

测试当前关卡复用设计师保存前校验：

- 必须先生成地图或导入 JSON。
- 地图不能为空。
- 每一行长度必须一致。
- 宽度必须在 5 到 150。
- 高度必须在 5 到 150。
- 必须有且只有一个起点 `2`。
- 至少有一个终点 `3`。
- 地图字符必须是已知元素。
- 如果存在门 `A`，必须至少存在钥匙 `K`。
- 候选点不能越界。
- 候选点不能重复。
- 候选点不能放在墙、起点、终点、死亡区、数据碎片、钥匙、门、旧弹射块、旧传送带上。

