# 直接支撑判定 + 内凹角脱困修正版

## 我重新检查后的原因

之前一直只修“投影范围”和“预留距离”，但这次真正的问题是：

```text
宽投影函数把平台侧边也误判成了上方/下方支撑
```

具体来说：

- `isTouchingWallAbove()` 会扫描整个小球水平投影上方有没有墙；
- 在内凹角位置，球正上方其实已经没有真正的天花板支撑；
- 但是球侧边的投影仍然扫到了平台侧面；
- 于是程序以为“上方还有支撑”；
- `applyGravityAfterLeavingWall()` 就不会让它向上掉；
- 同时 y 方向移动又被角落挡住，所以卡死。

所以问题不是单纯“球太大”，也不是单纯“探出距离不够”，而是：

```text
宽投影支撑 ≠ 真正直接支撑
```

## 本次修改

修改文件：

- `gamescene.h`
- `gamescene.cpp`

新增了两个直接支撑判定：

```cpp
bool hasDirectSupportAbove() const;
bool hasDirectSupportBelow() const;
```

它们只看：

```text
球心正上方 / 球心正下方
```

是否真的有支撑。

现在逻辑变成：

```text
宽投影：用于保证球在平台边缘不要太早掉落
直接支撑：用于判断内凹角是不是误判成支撑
```

如果：

```text
宽投影说有支撑
但球心正上/正下没有直接支撑
```

那就认为这是内凹角误判，会启动脱困：

```text
向左/右小幅寻找可坠落位置
然后按当前重力方向继续坠落
```

## 我做的代码级验证

我检查了这几个关键路径：

1. `moveBallOneStep()` 中 `velocity.y != 0` 且被角落挡住时，不再直接 `velocity.setY(0)`。
2. 它会先检查 `hasDirectSupportAbove/Below()`。
3. 如果没有直接支撑，会调用 `tryEscapeCornerAndFall()`。
4. `applyGravityAfterLeavingWall()` 不再只相信宽投影结果。
5. 宽投影有支撑但直接支撑没有时，会主动触发脱困坠落。

## 验收方法

### 验收 1：你截图里的这个角

1. 使用你当前截图里的地图。
2. 让小球重力向上。
3. 让小球移动到这个内凹角。
4. 小球不应该再卡死。
5. 它应该从角落小幅脱出，然后继续向上坠落。

### 验收 2：上方墙左右两个方向

1. 重力向上。
2. 贴着上方墙从左往右走到边缘。
3. 应该向上坠落，不应该卡住。
4. 再从右往左测一次，也应该正常。

### 验收 3：下方平台左右两个方向

1. 重力向下。
2. 沿下方平台从左往右滚出边缘。
3. 应该向下坠落，不应该卡住。
4. 再从右往左测一次，也应该正常。

### 验收 4：正常撞墙仍然正常

1. 让球正面撞墙。
2. 应该停住。
3. 不应该反弹。
4. 不应该穿墙。

### 验收 5：左右墙仍然不吸附

1. 让球碰到左墙或右墙。
2. 没有上/下支撑时，不应该沿左右墙滚。
3. 不应该挂在左右墙上。
