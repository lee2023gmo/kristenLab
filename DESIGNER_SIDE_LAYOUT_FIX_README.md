# 关卡设计师左右布局修正版

## 修正原因

原来的关卡设计师是：

```text
上方整行：关卡设置
中间整行：地图
下方整行：操作按钮
```

当地图区域被自动放大后，下方按钮容易被挤到窗口外，看起来像“操作框没了”。
而上方设置项其实不需要占满整行宽度，浪费了很多空间。

## 本次修改

将关卡设计师改为左右布局：

```text
左侧：关卡设置 + 当前工具 + 操作按钮 + 操作提示
右侧：地图编辑区域 + 地图字符串预览
```

左侧栏固定宽度约 340 像素，按钮不再放在底部横栏，所以地图变大时不会把按钮挤没。

## 修改文件

- `leveleditordialog.cpp`

## 验收方法

1. 打开 `关卡设计师`。
2. 检查窗口是否变成左右布局：
   - 左边是设置和按钮；
   - 右边是地图。
3. 点击 `导入 JSON` 导入你截图里的地图。
4. 检查操作按钮是否仍然可见，不会跑到窗口外。
5. 检查地图字符串预览是否仍然在右侧地图下方显示。
6. 设置 12×8、20×12、30×20 分别点击 `生成地图`：
   - 左侧工具栏仍然可见；
   - 按钮仍然可点；
   - 地图区域根据大小扩展；
   - 不会再出现底部操作框消失的问题。
7. 检查左键拖动绘制、右键擦除、保存 JSON 是否仍然正常。
