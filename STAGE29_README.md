# Stage 29 修改说明

本压缩包基于阶段 28 继续修改。

## 已实现

阶段 29：关卡设计师体验优化。

新增/优化功能：

1. 当前工具高亮
   - 下拉框选择元素后，会在“工具预览”区域显示当前工具。
   - 预览区颜色会跟随当前元素变化。

2. 鼠标拖动连续绘制
   - 左键按住并拖动：连续铺设当前选择的元素。
   - 适合快速铺墙、铺死亡区、铺机关。

3. 右键擦除为空地
   - 右键点击或右键拖动：格子变回空地 `0`。
   - 保留了阶段 25 的“双击左键擦除”作为备用操作。

4. 一键清空地图
   - 新增按钮 `清空为空地`。
   - 会询问确认，然后把整张地图全部变成空地 `0`。

5. 一键自动边框墙
   - 保留阶段 26 的 `自动加边框墙`。
   - 点击后第一行、最后一行、第一列、最后一列全部变成墙体 `1`。

6. 底部实时地图字符串预览
   - 表格下方会实时显示当前地图字符串。
   - 绘制、擦除、导入、清空、加边框墙都会刷新预览。

7. 保存后提示完整路径，并询问是否立即进入关卡选择
   - 选择“是”后，会关闭关卡设计师并打开关卡选择界面。

## 修改文件

- `leveleditordialog.h`
- `leveleditordialog.cpp`
- `mainwindow.cpp`

`gamescene.cpp` 本阶段不需要修改。

## 验收方式

1. 打开关卡设计师。
2. 点击 `生成地图`。
3. 切换不同工具，确认工具预览会变化。
4. 按住左键拖动，确认可以连续绘制。
5. 按住右键拖动，确认可以连续擦除为空地。
6. 点击 `清空为空地`，确认整张图变成 `0`。
7. 点击 `自动加边框墙`，确认最外圈变成墙体。
8. 观察底部地图字符串预览是否实时变化。
9. 保存合法地图，确认弹窗显示完整路径，并可选择立即进入关卡选择界面。
