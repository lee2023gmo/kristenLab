# 小球尺寸缩小 + 角落卡死修正版

## 修改原因

你截图里的问题是：

- 小球重力向上；
- 贴着上方墙从左往右移动；
- 到达内凹角落时，小球视觉圆边缘和墙角仍有轻微重叠；
- 导致小球卡死，没有顺利向上坠落。

你提出的方案是把球的直径设置得比格子边长更小，比如 0.8 倍或 0.6 倍。

## 本次修改

修改了 `constants.h`：

```cpp
const int BALL_RADIUS = 12;
```

原来是：

```cpp
const int BALL_RADIUS = 14;
```

现在：

- 格子边长 `TILE_SIZE = 40`
- 小球直径 `BALL_RADIUS * 2 = 24`
- 小球直径约等于格子边长的 `0.6` 倍

也就是：

```text
24 / 40 = 0.6
```

## 同时保留之前的碰撞优化

`gamescene.cpp` 里仍然使用略小的碰撞半径：

```cpp
collisionRadius = ball.radius - 4
```

因此：

- 视觉小球更小
- 实际碰撞体也更小
- 平台边缘、内凹角落、上方墙断开处更不容易卡死

## 修改文件

- `constants.h`
- `gamescene.cpp`

## 验收方法

### 验收 1：你截图里的向上角落

1. 使用你截图里的地图。
2. 让小球重力向上。
3. 让小球贴着上方墙从左往右移动到内凹角落。
4. 小球不应该卡在角落。
5. 当上方支撑断开后，小球应该向上坠落。

### 验收 2：从右往左贴上方墙

1. 让小球重力向上。
2. 让小球从右往左贴着上方墙移动。
3. 经过上方平台边缘时，不应该卡死。
4. 离开上方支撑后应向上坠落。

### 验收 3：下方平台边缘

1. 让小球重力向下。
2. 让小球沿下方平台从左往右滚出平台。
3. 小球不应该卡在平台边缘。
4. 离开下方支撑后应向下坠落。

### 验收 4：基础规则不变

1. 正面撞墙仍然不反弹。
2. 左右墙仍然不吸附。
3. 四面没有上/下支撑时，不能切换方向。
4. 贴着下方墙时，仍然可以左右滚和向上离开。
5. 贴着上方墙时，仍然可以左右滚和向下离开。
