# 传送门 + 测试当前关卡合并说明

本版本在“钥匙 K / 门 A + 撤销重做 + 左侧滚动适配”的基础上继续补齐：

1. 关卡设计师里的“测试当前关卡”按钮。
2. 新地图元素：传送门 `B`。

## 一、测试当前关卡

关卡设计师左侧按钮区保留：

- 校验地图
- 测试当前关卡
- 保存关卡

点击“测试当前关卡”后：

1. 先执行设计器地图校验。
2. 校验通过后，把当前表格内容临时组装成 `Level`。
3. 不写入 JSON 文件。
4. 关闭设计器，进入游戏界面试玩临时关卡。

相关代码：

- `LevelEditorDialog::testCurrentLevel()`
- `LevelEditorDialog::requestTestLevel(const Level &level)`
- `MainWindow::setupGameWindowForTestLevel(const Level &level)`
- `GameScene::loadTemporaryLevelForTest(const Level &level)`

## 二、传送门 B

新增地图元素：

```text
B = 传送门
```

玩法规则：

1. 角色进入传送门格子后，自动传送到对应传送门的中心位置。
2. 传送门按地图扫描顺序配对：从上到下、从左到右读取，
   第 1 个和第 2 个配对，第 3 个和第 4 个配对，以此类推。
3. 传送后会锁定目标传送门，直到角色离开该传送门格子，避免刚传过去就立刻传回来。
4. 传送会保留当前速度和重力方向。

示例：

```text
B1 <-> B2
B3 <-> B4
```

## 三、设计器支持

关卡设计师元素下拉框新增：

```text
传送门 B
```

表格里传送门显示为紫色 `B`。

## 四、校验标准

在原有校验基础上新增：

1. `B` 是合法地图字符。
2. 传送门数量可以为 0。
3. 如果存在传送门，数量必须至少为 2。
4. 传送门数量必须为偶数。
5. 候选编辑点不能放在传送门上。

其他保留校验：

1. 地图不能为空。
2. 每行宽度必须一致。
3. 编辑器宽高范围仍为 5 ~ 150。
4. 必须有且只有一个起点 `2`。
5. 至少有一个终点 `3`。
6. 如果存在门 `A`，必须至少存在一把钥匙 `K`。
7. 候选编辑点不能越界、不能重复、不能放在墙、起点、终点、死亡区、数据碎片、钥匙、门、传送门、弹射块、传送带上。

## 五、示例关卡

新增：

```text
levels/portal_demo.json
```

用于测试传送门基础行为。
