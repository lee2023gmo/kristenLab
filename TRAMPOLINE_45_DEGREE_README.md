# 蹦床 45° 斜向上反弹修正版

## 修改目标

把蹦床的反弹方向从“垂直向上”改成：

```text
45° 斜向右上
```

## 修改文件

- `gamescene.cpp`

## 新规则

### 1. 竖直向下落到蹦床

如果小球竖直向下落到蹦床 `9 / T`：

```cpp
velocity = QPointF(moveSpeed, -moveSpeed);
```

也就是：

```text
向右 + 向上
```

视觉效果是：

```text
45° 斜向右上弹出
```

### 2. 竖直向上撞到蹦床

为了对称处理，如果小球竖直向上撞到蹦床：

```text
45° 斜向右下弹出
```

### 3. 空中不能改变方向

蹦床弹出后，小球处于空中斜向运动状态。

如果没有贴着上方或下方支撑：

```text
方向键不应改变方向
```

### 4. 碰到墙面直接停下

蹦床斜向弹出的球一旦碰到墙面：

```text
速度变成 0
直接停下
不反弹
不吸附
不继续沿墙滑动
```

## 为什么需要额外修改

之前 `applyGravityAfterLeavingWall()` 会把“重力向上但没有上方支撑”的速度强制改成纯竖直向上：

```cpp
velocity = QPointF(0, -moveSpeed);
```

这会把蹦床的 45° 斜向速度覆盖掉。

本次已加判断：

```cpp
if (velocity.x() != 0 && velocity.y() != 0) {
    return;
}
```

这样蹦床弹出的斜向速度不会被改回竖直速度。

同时，`moveBallOneStep()` 里增加了斜向空中碰墙停止逻辑，保证碰墙后直接停下。

## 验收方法

### 验收 1：竖直下落后 45° 右上弹出

1. 做一张简单地图。
2. 在小球正下方放置蹦床 `9 / T`。
3. 让小球重力向下，竖直落到蹦床上。
4. 小球碰到蹦床后不应再垂直向上弹。
5. 应该沿 45° 右上方向飞出。

### 验收 2：空中不能改变方向

1. 小球被蹦床弹出后，在空中按方向键。
2. 如果小球没有贴着上方或下方墙面，方向不应该改变。
3. 状态应仍然类似 `空中不可切换`。

### 验收 3：碰墙直接停下

1. 在蹦床右上方布置一堵墙。
2. 让小球落到蹦床后 45° 右上飞出。
3. 小球碰到墙后应直接停下。
4. 不应该反弹。
5. 不应该吸附到左右墙。
6. 不应该沿墙滑动。

### 验收 4：水平经过不触发

1. 让小球水平经过蹦床。
2. 如果不是竖直进入蹦床，不应触发弹跳。

### 验收 5：原有规则不被破坏

1. 普通撞墙仍然停住，不反弹。
2. 贴着下方墙仍能左右滚和向上离开。
3. 贴着上方墙仍能左右滚和向下离开。
4. 左右墙仍不提供吸附。
