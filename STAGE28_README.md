# Stage 28 修改说明

本压缩包基于阶段 27 项目根目录保存修正版继续修改。

## 已实现

阶段 28：导入已有 JSON 继续编辑。

现在在“关卡设计师”窗口中可以：

1. 点击 `导入 JSON`
2. 使用 `QFileDialog::getOpenFileName()` 选择已有 `.json` 关卡
3. 读取 `name`、`targetReverseCount`、`map`
4. 自动把关卡名填入输入框
5. 自动把目标反转次数填入输入框
6. 根据 `map` 的行数和列数生成表格
7. 把每个地图字符加载到对应格子
8. 刷新格子的文字和颜色
9. 继续修改地图
10. 修改后默认保存到 `custom_levels`

## 导入校验

导入时会检查：

- 文件能否打开
- JSON 是否能解析
- JSON 根节点是否是对象
- 是否存在 `map` 数组
- `map` 是否为空
- `map` 每一行是否都是字符串
- 地图是否有且只有一个起点
- 地图是否至少有一个终点
- 地图是否有非法字符
- 地图每一行长度是否一致
- 地图尺寸是否在宽 5~30、高 5~20 范围内

## 验收方式

1. 打开关卡设计师
2. 点击 `导入 JSON`
3. 选择 `levels` 或 `custom_levels` 里的已有关卡
4. 检查关卡名、目标反转次数、表格大小、表格内容是否正确
5. 修改几个格子
6. 保存位置默认应回到 `custom_levels`
7. 点击保存，可以另存为自定义关卡
