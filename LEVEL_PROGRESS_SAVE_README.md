# 关卡进度保存功能

本次在当前版本基础上新增了关卡进度保存系统，重点用于让游戏形成“通关记录 + 解锁下一关 + 最佳成绩显示”的闭环。

## 新增文件

- `progressmanager.h`
- `progressmanager.cpp`

`ProgressManager` 负责读取、写入和查询本地通关进度。

## 保存位置

运行时会自动创建：

```text
save/progress.json
```

路径定位规则和 `levels` / `custom_levels` 类似：如果程序运行在 Qt Creator 的 build 目录下，会尽量回到项目根目录，再写入 `save/progress.json`。

该文件属于本地玩家进度，不建议提交到 Git，因此 `.gitignore` 中新增了：

```text
save/progress.json
```

## 保存内容

每次正式关卡通关后会保存：

- 是否已经通关
- 历史最高星级
- 历史最快用时
- 历史最少反转次数
- 历史最少死亡次数
- 最近一次通关时间
- 已解锁到第几关

临时测试关卡不会写入进度。

## 解锁规则

- 第 1 个内置关卡默认解锁。
- 通关第 N 个内置关卡后，自动解锁第 N+1 个内置关卡。
- 自定义关卡始终可玩，但也会保存最佳成绩。
- 按 N / 下一关按钮时，也会遵守解锁规则，不能跳过未解锁关卡。

## 关卡选择界面变化

关卡选择按钮现在会显示第二行进度信息，例如：

```text
第 1 关：基础关卡
★★☆  |  最快 12.35 秒  |  最少反转 4  |  最少死亡 0
```

未通关但已解锁的关卡显示：

```text
已解锁 · 未通关
```

未解锁的内置关卡会被禁用，并显示：

```text
🔒 未解锁
```

## 通关结算界面变化

正式关卡通关后，结算界面会提示成绩已保存，并显示当前关卡的历史最佳成绩。

## 主要修改文件

- `gamescene.cpp`
  - 通关时调用 `ProgressManager::updateAfterCompletion()` 保存成绩。
  - 下一关/手动加载关卡时检查是否已解锁。
  - 通关结算界面显示历史最佳成绩。

- `mainwindow.cpp`
  - 关卡选择界面读取进度并显示每关成绩。
  - 未解锁内置关卡按钮禁用。

- `CMakeLists.txt`
  - 加入 `progressmanager.cpp / progressmanager.h`。

## 校验建议

1. 删除 `save/progress.json` 后启动游戏。
2. 打开关卡选择，第 1 个内置关卡应可点击，后续内置关卡应显示未解锁。
3. 通关第 1 关，结算界面应显示“成绩已保存”。
4. 返回关卡选择，第 1 关应显示星级和最佳成绩，第 2 关应变为可点击。
5. 重玩第 1 关，如果用时更短或死亡更少，应刷新对应最佳记录。
6. 自定义关卡应始终可点击，通关后也应保存最佳成绩。
