# 游戏关卡界面缩放和滚动条修正版

## 修改原因

设计师模式已经支持 80×80、150×150 大地图缩放和滚动，但是进入正式关卡后，`QGraphicsView` 原来关闭了滚动条：

```cpp
gameView->setHorizontalScrollBarPolicy(Qt::ScrollBarAlwaysOff);
gameView->setVerticalScrollBarPolicy(Qt::ScrollBarAlwaysOff);
```

所以大地图在游戏中显示不下，也没有办法查看边角。

## 本次修改

### 1. 游戏界面开启横向和纵向滚动条

现在游戏关卡中的 `QGraphicsView` 会强制显示：

```text
横向滚动条
纵向滚动条
```

可以拖动查看整张地图的边角。

### 2. 游戏界面新增视图缩放

底部按钮新增：

```text
缩小视图
放大视图
还原视图
适应窗口
```

顶部状态栏新增：

```text
视图：xx%
```

### 3. 自动适应大地图

进入关卡后会自动执行一次：

```text
适应窗口
```

大地图会自动缩小到能尽量看全。

如果你想精细操作，可以点击：

```text
放大视图
```

然后用横向/纵向滚动条浏览地图边角。

### 4. 修正按钮重复添加问题

原先底部按钮区有一组编辑按钮被重复添加了一次，本次顺便整理成两行按钮：

第一行：

```text
进入编辑模式 / 弹射块 / 缓冲区 / 传送带 / 蹦床 / 开始运行 / 保存地图
```

第二行：

```text
缩小视图 / 放大视图 / 还原视图 / 适应窗口 / 上一关 / 重开 / 暂停 / 下一关 / 返回主菜单
```

## 修改文件

- `mainwindow.h`
- `mainwindow.cpp`

## 验收方法

### 验收 1：大地图进入关卡能缩放

1. 在关卡设计师中生成或保存一张 `80×80` 或 `150×150` 地图。
2. 从关卡选择进入该关。
3. 进入后地图应自动缩小，顶部状态栏显示类似 `视图：20%` 或更小。
4. 应能看到比之前更多的地图范围。

### 验收 2：横向和纵向滚动条可用

1. 进入大地图关卡。
2. 观察游戏地图区域。
3. 应看到横向滚动条和纵向滚动条。
4. 拖动横向滚动条到最右边，应能看到地图最右侧。
5. 拖动纵向滚动条到最下方，应能看到地图最下侧。
6. 四个角都应能通过滚动条浏览到。

### 验收 3：手动缩放

1. 点击 `放大视图`。
2. 地图应变大，顶部 `视图：xx%` 应增加。
3. 点击 `缩小视图`。
4. 地图应变小，顶部 `视图：xx%` 应减少。
5. 点击 `还原视图`。
6. 地图应回到 100%。
7. 点击 `适应窗口`。
8. 地图应再次缩放到适合窗口的比例。

### 验收 4：游戏操作不受影响

1. 缩放后点击 `开始运行`。
2. 小球移动、重力切换、蹦床、碰墙停住等逻辑应正常。
3. 缩放只是显示层变化，不应改变地图数据和碰撞逻辑。

### 验收 5：切换关卡后自动适应

1. 在一个大地图关卡中点击 `下一关` 或 `上一关`。
2. 新关卡加载后应自动重新适应窗口。
3. 仍可手动缩放和滚动。
