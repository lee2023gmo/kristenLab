# 钥匙门 + 设计师撤销重做 + 左侧滚动布局合并说明

本版本修复了上一版钥匙门功能覆盖掉关卡设计师撤销/重做和左侧自适应布局的问题。

## 修复内容

1. 保留钥匙与门机制：
   - `K`：钥匙，角色进入该格后自动拾取。
   - `A`：门，未拾取钥匙时阻挡，拾取至少一把钥匙后打开并可通过。

2. 恢复关卡设计师撤销 / 重做：
   - `Ctrl + Z`：撤销。
   - `Ctrl + Y`：重做。
   - `Ctrl + Shift + Z`：重做。
   - 左侧新增“撤销 / 重做”按钮。

3. 恢复左侧工具栏滚动布局：
   - 左侧工具区放入 `QScrollArea`。
   - 窗口高度不足时左侧可以滚动，不再压缩按钮和输入框。
   - 输入框、下拉框、按钮提高最小高度，避免文字被裁切。

## 撤销/重做覆盖的操作

- 生成地图。
- 导入 JSON。
- 左键绘制元素。
- 右键擦除元素。
- 绘制/删除候选点。
- 添加边框墙。
- 清空地图。
- 清空候选点。

连续拖动绘制会作为一次撤销入口保存，按一次 `Ctrl + Z` 可以回到拖动前状态。

## 钥匙门校验规则

- 地图字符 `K` 和 `A` 为合法字符。
- 如果地图中存在门 `A`，必须至少存在一把钥匙 `K`。
- 候选点不能放在钥匙 `K` 或门 `A` 上。

