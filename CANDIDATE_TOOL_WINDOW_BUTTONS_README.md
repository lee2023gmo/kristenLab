# 候选点工具化 + 设计师窗口修正

## 本次修改

### 1. 设计师窗口不再铺满屏幕

之前“适应窗口”被实现成了几乎填满整个屏幕。现在改为：

```text
只保证窗口在屏幕可见范围内
不主动铺满整个屏幕
保持正常窗口大小
```

同时保留：

```text
最小化按钮
最大化按钮
关闭按钮
右下角缩放手柄
底部拖动条
```

### 2. 候选点变成真正的地图设计工具

元素下拉框新增：

```text
候选点 E
```

使用方式和普通元素一样：

```text
左键点击 / 拖动：布置候选点
右键点击 / 拖动：删除候选点
双击：删除候选点
```

这不会改变地图字符，只会写入 JSON 的：

```json
"editablePoints": [
  { "row": 3, "col": 5 }
]
```

### 3. 修复“候选点模式开了但布置不了”的问题

原因是旧版本候选点模式依赖当前元素，但当前元素经常还是 `空地 0`，所以左键被误判成“删除候选点”。

现在逻辑改为：

```text
选择候选点 E 后，左键一定是添加候选点
右键一定是删除候选点
```

### 4. 保留旧按钮

原来的 `候选点模式` 按钮保留，但现在它相当于快捷按钮：

```text
点击后自动选择“候选点 E”工具
```

推荐以后直接用元素下拉框中的 `候选点 E`。

## 修改文件

- `leveleditordialog.h`
- `leveleditordialog.cpp`

## 验收方法

### 验收 1：窗口不再铺满屏幕

1. 打开 `关卡设计师`。
2. 窗口不应该自动铺满整个屏幕。
3. 窗口应该保持正常大小，并且标题栏在屏幕内。
4. 右上角应有最小化、最大化、关闭按钮。
5. 右下角缩放手柄仍然可用。
6. 底部拖动条仍然可拖动窗口。

### 验收 2：候选点作为元素出现

1. 打开 `关卡设计师`。
2. 查看左侧 `元素` 下拉框。
3. 应能看到 `候选点 E`。
4. 选择 `候选点 E` 后，预览应显示 `当前工具：候选点 E`。

### 验收 3：候选点可以正常布置

1. 生成或导入一张地图。
2. 在元素下拉框选择 `候选点 E`。
3. 在空地、弹射块、缓冲区、传送带、蹦床上左键点击或拖动。
4. 对应格子应变成黄色 `E` 或 `B* / T* / ↗*`。
5. 左侧候选点数量应增加。

### 验收 4：候选点可以删除

1. 保持 `候选点 E` 工具。
2. 在已有候选点上右键点击或右键拖动。
3. 候选点标记应消失。
4. 左侧候选点数量应减少。
5. 双击候选点也应能删除。

### 验收 5：候选点不能放在非法元素上

1. 选择 `候选点 E`。
2. 尝试点击墙体、起点、终点、死亡区、数据碎片。
3. 这些格子不应变成候选点。
4. 候选点数量不应增加。

### 验收 6：保存和游戏内编辑

1. 布置几个候选点。
2. 保存关卡。
3. 打开 JSON，确认存在 `editablePoints`。
4. 进入该关卡。
5. 点击 `进入编辑模式`。
6. 只有这些候选点能被玩家编辑。
