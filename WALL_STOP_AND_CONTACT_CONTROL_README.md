# 撞墙停止 + 贴墙才允许切换重力 说明

## 修改目标

本版本修改了小球运动规则：

1. 小球碰到墙后不再反弹，而是直接停在那里。
2. 小球只有在贴着墙时，才能改变重力方向。
3. 如果小球四面都没有贴墙，也就是处在空中，就不能改变重力方向。
4. 贴着墙时，可以：
   - 沿着墙滚动
   - 向墙的反方向离开

## 具体规则

### 贴着上方墙

允许：

- 向下
- 向左
- 向右

不允许：

- 向上继续顶墙

### 贴着下方墙

允许：

- 向上
- 向左
- 向右

不允许：

- 向下继续顶墙

### 贴着左侧墙

允许：

- 向右
- 向上
- 向下

不允许：

- 向左继续顶墙

### 贴着右侧墙

允许：

- 向左
- 向上
- 向下

不允许：

- 向右继续顶墙

### 四面都不贴墙

不允许切换重力方向。

## 修改文件

- `gamescene.h`
- `gamescene.cpp`

## 验收方法

### 验收 1：撞墙不反弹

1. 打开任意关卡。
2. 让小球朝墙移动。
3. 小球碰到墙后应该停住。
4. 不应该像以前一样反弹回来。

### 验收 2：空中不能切换方向

1. 让小球在空中下落或移动。
2. 在小球没有碰到任何墙时，按方向键。
3. 小球方向不应该改变。
4. 状态栏应该显示类似“空中不可切换”。

### 验收 3：贴着下方墙可以左右滚动

1. 让小球落到地面墙上。
2. 按左方向键。
3. 小球应该能向左滚动。
4. 再让它贴着地面墙，按右方向键。
5. 小球应该能向右滚动。

### 验收 4：贴着下方墙可以向墙反方向离开

1. 让小球停在地面墙上。
2. 按上方向键。
3. 小球应该能向上离开地面墙。
4. 但如果按下方向键，小球不应该继续向墙里移动。

### 验收 5：贴着上方墙可以左右滚动或向下离开

1. 让小球顶到上方墙。
2. 小球应该停住。
3. 按左或右，小球应该能沿着上方墙移动。
4. 按下，小球应该能离开上方墙。
5. 按上，小球不应该继续向墙里移动。

## 调试提示

如果某次按键没有生效，可以看 Qt Creator 的“应用程序输出”。

如果看到：

```text
Gravity change denied
```

说明这次方向切换被规则拒绝了。
