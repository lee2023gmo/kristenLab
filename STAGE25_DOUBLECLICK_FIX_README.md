# Stage 25 双击擦除修正版

本压缩包基于阶段 25 修改。

## 修正内容

原来的版本使用：

- 右键点击格子 → 擦除为空地 `0`

但是在部分系统、触控板或 Qt 表格事件处理中，右键事件可能不会稳定触发。

现在改为：

- 左键单击格子 → 绘制当前选择的元素
- 左键双击格子 → 擦除为空地 `0`

## 验收方式

1. 主菜单点击 `关卡设计师`
2. 点击 `生成地图`
3. 在下拉框选择 `墙体 1`
4. 左键单击某个格子，格子变成墙体
5. 左键双击这个格子，格子变回空地 `0`
6. 选择 `起点 S`
7. 点击多个格子，最终仍然只保留一个起点

## 修改文件

- `leveleditordialog.h`
- `leveleditordialog.cpp`

## 注意

本修正版没有进入阶段 26。
只是修正阶段 25 的擦除交互。
