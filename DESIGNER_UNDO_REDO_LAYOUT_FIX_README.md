# 关卡设计师撤销/重做与布局重叠修复

## 修改目标

本次修改解决关卡设计师左侧工具区在新增按钮后发生控件挤压、文字截断和按钮重叠的问题，同时加入撤销/重做功能。

## 主要修改

### 1. 左侧工具区改为可滚动区域

左侧参数、元素说明和按钮区域现在放入 `QScrollArea`。

当窗口高度不够时，左侧会出现滚动条，而不是继续压缩输入框和按钮。

### 2. 提高控件最小高度

统一提高输入框、下拉框、SpinBox、提示标签和按钮的最小高度，避免高 DPI 或字体较大时文字被裁切。

### 3. 新增撤销/重做

支持：

```text
Ctrl + Z：撤销
Ctrl + Y：重做
Ctrl + Shift + Z：重做
```

左侧按钮区新增：

```text
撤销
重做
```

### 4. 拖动绘制合并为一步

按住左键或右键连续拖动绘制时，会在鼠标按下前记录一次快照。

因此拖动一整段墙体后，只需要撤销一次即可整体回退。

## 覆盖的操作

目前支持撤销/重做：

```text
生成地图
导入 JSON
左键绘制元素
右键擦除元素
双击擦除元素
添加/删除候选点
添加边框墙
清空地图
清空候选点
```

## 修改文件

```text
leveleditordialog.h
leveleditordialog.cpp
```
