# Stage 25 修改说明

本压缩包基于阶段 24 继续修改。

## 已实现

阶段 25：关卡元素绘制工具。

现在在“关卡设计师”窗口中可以：

1. 使用下拉框选择当前绘制元素
2. 点击表格格子，把格子改成当前元素
3. 右键点击格子，擦除为空地 `0`
4. 起点 `2` 会特殊处理：整张地图只能保留一个起点
5. 不同元素会显示不同文字和颜色

## 元素对应关系

- 空地：`0`，显示 `0`
- 墙体：`1`，显示 `1`
- 起点：`2`，显示 `S`
- 终点：`3`，显示 `END`
- 死亡区：`4`，显示 `X`
- 弹射块：`5`，显示 `B`
- 缓冲区：`6`，显示 `SLOW`
- 传送带：`7`，显示 `→`
- 数据碎片：`8`，显示 `*`

## 验收方式

运行程序后：

1. 主菜单点击 `关卡设计师`
2. 点击 `生成地图`
3. 在下拉框选择 `墙体 1`
4. 点击表格格子，格子应变成墙体
5. 在下拉框选择 `起点 S`
6. 点击两个不同格子，最终只能保留一个起点
7. 右键点击任意格子，格子应变回空地 `0`

## 注意

阶段 25 不做保存 JSON。
阶段 26 会继续实现设计师模式地图校验。
