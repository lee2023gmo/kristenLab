# 水平向左 / 向右蹦床

## 本次实现

在原有四个斜向蹦床基础上，新增两个水平蹦床：

| 显示 | JSON 字符 | 效果 |
|---|---|---|
| → | `r` | 水平向右弹出 |
| ← | `l` | 水平向左弹出 |

原有蹦床保持不变：

| 显示 | JSON 字符 | 效果 |
|---|---|---|
| ↗ | `9` | 45° 右上 |
| ↖ | `u` | 45° 左上 |
| ↘ | `v` | 45° 右下 |
| ↙ | `w` | 45° 左下 |

## 修改文件

- `tiledefs.h`
- `gamescene.h`
- `gamescene.cpp`
- `leveleditordialog.cpp`
- `mainwindow.cpp`

## 规则说明

1. 小球必须竖直进入蹦床才触发。
2. 水平滚过蹦床不触发。
3. 水平蹦床触发后，小球会沿箭头方向水平飞出。
4. 水平飞出时属于“空中弹射运动”，不会被重力逻辑立刻改回下落。
5. 飞行途中不能改变方向。
6. 碰到墙面后直接停下，不反弹、不滑墙。

## 验收方法

### 验收 1：设计师能放置水平蹦床

1. 打开 `关卡设计师`。
2. 展开左侧 `元素` 下拉框。
3. 应能看到：
   - `蹦床 →`
   - `蹦床 ←`
4. 选择 `蹦床 →`，点击地图格子，格子应显示 `→`。
5. 选择 `蹦床 ←`，点击地图格子，格子应显示 `←`。

### 验收 2：保存 JSON 字符正确

1. 在地图中放置 `蹦床 →` 和 `蹦床 ←`。
2. 保存关卡。
3. 打开保存的 JSON。
4. `蹦床 →` 应保存为字符 `r`。
5. `蹦床 ←` 应保存为字符 `l`。

### 验收 3：游戏内显示正确

1. 从关卡选择进入刚保存的关卡。
2. 地图中的水平蹦床应显示为：
   - `→`
   - `←`

### 验收 4：向右水平弹出

1. 放置一个 `蹦床 →`。
2. 让小球竖直下落到该蹦床。
3. 小球触发后应水平向右飞出。
4. 不应斜向飞，也不应立刻向下掉。

### 验收 5：向左水平弹出

1. 放置一个 `蹦床 ←`。
2. 让小球竖直下落到该蹦床。
3. 小球触发后应水平向左飞出。
4. 不应斜向飞，也不应立刻向下掉。

### 验收 6：水平经过不触发

1. 让小球从左到右或从右到左水平经过 `→ / ←` 蹦床。
2. 如果不是竖直进入，蹦床不应触发。
3. 小球应按原运动方向继续。

### 验收 7：碰墙停止

1. 在水平蹦床飞出方向前方放一堵墙。
2. 让小球触发水平蹦床。
3. 小球碰到墙后应直接停下。
4. 不应反弹。
5. 不应沿墙滑动。

### 验收 8：玩家编辑模式按钮

1. 进入带候选点的关卡。
2. 点击 `进入编辑模式`。
3. 底部应出现：
   - `蹦床→`
   - `蹦床←`
4. 点击 `蹦床→` 后点击候选点，候选点应直接变成 `→` 蹦床。
5. 点击 `蹦床←` 后点击候选点，候选点应直接变成 `←` 蹦床。
