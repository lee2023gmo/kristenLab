# 关卡设计师地图缩放修正版

## 修正原因

30×20 地图在 42px 单元格下会超过右侧表框，需要滚动很多，也不方便拖动绘制。

## 本次修改

在左侧按钮区新增：

- `缩小地图`
- `放大地图`
- `还原缩放`

并新增缩放显示，例如：

```text
28 px / 67%
```

## 缩放规则

默认单元格边长：

```text
42 px
```

可调范围：

```text
20 px ~ 48 px
```

每次点击放大或缩小：

```text
±4 px
```

## 自动适配

生成或导入地图时会自动适配初始缩放：

- 30×20：自动使用 28px
- 中等地图：自动使用 32px
- 小地图：使用 42px

用户之后仍然可以手动缩放。

## 验收方法

1. 打开 `关卡设计师`。
2. 设置宽度 `30`，高度 `20`。
3. 点击 `生成地图`。
4. 地图应自动缩小，缩放显示应约为 `28 px / 67%`。
5. 右侧表框不应明显溢出。
6. 点击 `缩小地图`，单元格继续变小。
7. 点击 `放大地图`，单元格变大。
8. 点击 `还原缩放`，单元格恢复 `42 px / 100%`。
9. 缩放后继续测试：
   - 左键拖动绘制正常；
   - 右键拖动擦除正常；
   - 地图字符串预览正常；
   - 保存 JSON 正常。
