# 设计师模式：定义玩家编辑模式可编辑位置

## 本次实现目标

在关卡设计师里，除了设计地图元素，还可以直接定义：

```text
玩家进入编辑模式后，哪些格子可以放置 / 切换机关
```

这些位置会保存到 JSON 的：

```json
"editablePoints": [
  { "row": 3, "col": 5 },
  { "row": 3, "col": 6 }
]
```

游戏内的玩家编辑模式会读取这些点，并只允许玩家修改这些位置。

## 修改文件

- `leveleditordialog.h`
- `leveleditordialog.cpp`

## 新增功能

### 1. 新增“候选点模式”

左侧按钮新增：

```text
候选点模式：关 / 开
```

打开后：

- 左键点击或拖动：添加玩家可编辑候选点
- 右键点击或拖动：移除候选点
- 双击格子：移除候选点

关闭后：

- 恢复普通地图绘制模式

### 2. 新增“清候选点”

可以一键清空全部玩家编辑候选点。

### 3. 候选点显示

候选点会以黄色显示：

- 空地候选点显示为 `E`
- 机关候选点显示为 `B*`、`T*` 等

注意：这只是设计器显示效果，不会改变地图字符本身。

### 4. 保存 JSON

保存关卡时会写入：

```json
"editablePoints": [
  {
    "row": 1,
    "col": 3
  }
]
```

### 5. 导入 JSON

导入已有 JSON 时，如果文件里有 `editablePoints`，会自动恢复显示这些候选点。

## 候选点限制

候选点可以放在：

- 空地 `0`
- 弹射块 `5`
- 缓冲区 `6`
- 传送带 `7`
- 蹦床 `9`

候选点不能放在：

- 墙体 `1`
- 起点 `2`
- 终点 `3`
- 死亡区 `4`
- 数据碎片 `8`

这样和游戏内 `LevelManager` 的校验规则一致。

## 验收方法

### 验收 1：设计器能标记候选点

1. 打开 `关卡设计师`。
2. 生成一张地图。
3. 点击 `候选点模式：关`，让它变成 `候选点模式：开`。
4. 在几个空地格子上左键点击。
5. 这些格子应显示为黄色 `E`。
6. 左侧候选点数量应增加。

### 验收 2：右键可以删除候选点

1. 保持候选点模式开启。
2. 在已经标记为 `E` 的格子上右键点击。
3. 该格子应恢复原来的显示。
4. 候选点数量应减少。

### 验收 3：不能标在非法元素上

1. 在候选点模式下点击墙体、起点、终点、死亡区或数据碎片。
2. 这些格子不应变成候选点。
3. 候选点数量不应增加。

### 验收 4：保存 JSON 包含 editablePoints

1. 标记几个候选点。
2. 放置合法起点和终点。
3. 点击 `保存关卡`。
4. 打开生成的 JSON 文件。
5. 应看到 `editablePoints` 数组，里面有 row / col。

### 验收 5：导入 JSON 能恢复候选点

1. 重新打开关卡设计师。
2. 点击 `导入 JSON`，选择刚才保存的关卡。
3. 候选点应自动恢复为黄色 `E` 或 `B*`、`T*`。
4. 左侧候选点数量应正确。

### 验收 6：游戏内编辑模式只允许改这些点

1. 回到关卡选择界面，进入刚保存的关卡。
2. 点击 `进入编辑模式`。
3. 地图上应看到 `可改` 的候选点标记。
4. 点击候选点，应能切换弹射块 / 缓冲区 / 传送带 / 蹦床。
5. 点击非候选点，不应能修改。
