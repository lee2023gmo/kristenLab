# Stage 26 修改说明

本压缩包基于阶段 25 双击擦除修正版继续修改。

## 已实现

阶段 26：设计师模式地图校验。

现在在“关卡设计师”窗口中可以：

1. 从表格读取地图，生成 `QStringList mapData`
2. 点击 `校验地图`
3. 检查地图是否合法
4. 点击 `自动加边框墙`
5. 自动把第一行、最后一行、第一列、最后一列设置成墙体 `1`

## 校验规则

校验内容包括：

- 地图不能为空
- 每一行长度一致
- 必须有且只有一个起点 `2`
- 至少有一个终点 `3`
- 不能出现非法字符

## 新增按钮

新增按钮：

- `自动加边框墙`

点击后会执行：

- 第一行全部变成墙体 `1`
- 最后一行全部变成墙体 `1`
- 第一列全部变成墙体 `1`
- 最后一列全部变成墙体 `1`

## 验收方式

运行程序后：

1. 主菜单点击 `关卡设计师`
2. 点击 `生成地图`
3. 直接点击 `校验地图`
   - 应提示缺少起点
4. 放置一个起点 `S`
5. 再点击 `校验地图`
   - 应提示缺少终点
6. 放置一个终点 `END`
7. 再点击 `校验地图`
   - 应提示校验通过
8. 点击 `自动加边框墙`
   - 表格最外圈应全部变成墙体 `1`

## 注意

阶段 26 不做真正保存 JSON。
阶段 27 会继续实现保存为自定义关卡 JSON。
