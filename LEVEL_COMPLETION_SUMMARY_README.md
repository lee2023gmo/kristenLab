# 通关结算画面完善

本次改动在现有“测试当前关卡 / 撤销重做 / 左侧滚动适配 / 钥匙门 / 传送门”版本基础上，重做了通关后的结算展示。

## 修改文件

- `gamescene.h`
- `gamescene.cpp`

## 新增行为

通关后不再只弹出普通 `QMessageBox` 文本，而是显示一个更完整的结算窗口：

- 关卡完成标题
- 星级展示
- 关卡名
- 用时
- 反转次数 / 目标反转次数 / 是否达标
- 数据碎片收集情况
- 钥匙收集情况，若本关有钥匙
- 死亡次数
- 星级计算明细
- 操作按钮

## 操作按钮

普通关卡通关后：

- `停留查看`：关闭结算窗口，停留在当前结束画面
- `重玩本关`：重新开始当前关卡
- `下一关`：如果存在下一关，则直接进入下一关

测试关卡通关后：

- `关闭结果`：关闭结算窗口，停留在当前测试画面
- `重新测试`：重新开始当前测试关卡

最后一关通关后：

- `停留查看`
- `重玩本关`

## 星级计算规则

当前规则保持原有逻辑不变：

1. 完成关卡：+1 星
2. 反转次数小于等于目标反转次数：+1 星
3. 如果本关存在数据碎片，并且全部收集：+1 星

如果本关没有数据碎片，第三项不计星，因此该类关卡当前最高为 2 星。这一行为与原有 `calculateStars()` 逻辑保持一致。

## 设计原则

这次只完善“结算展示和通关后的按钮选择”，不改动原有胜利判定、星级算法、钥匙门、传送门、测试关卡、撤销重做和设计器布局逻辑。
