# 游戏界面科幻背景改造

本次改动将游戏运行界面调整为“科幻场景背景 + 中央悬浮终端 + 半透明 HUD”的效果。

## 改动内容

- 新增资源：`resources/images/game_interface_background.png`。
- 新增 `BackgroundGraphicsView`：
  - 背景固定在游戏视口中，不随关卡缩放或滚动；
  - 中央绘制悬浮终端背板，实际关卡继续由 `GameScene` 绘制；
  - 在实际地图外沿绘制跟随缩放的科技边框；
  - 使用完整视口刷新，避免缩放和滚动时产生背景残影。
- 顶部状态栏、底部按钮栏和滚动条改为半透明深蓝科技风格。
- “适应窗口”会把关卡放入中央终端的安全显示区域；手动放大后仍可通过滚动条查看。

背景图已加入 `resources.qrc`，不需要复制到程序运行目录。
