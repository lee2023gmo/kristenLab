# 关卡设计师宽高上限调整到 150

## 修改内容

本版本把关卡设计师里的地图尺寸上限改为：

```text
宽度：5 ~ 150
高度：5 ~ 150
```

原来是：

```text
宽度：5 ~ 30
高度：5 ~ 20
```

## 同步修改

同时修改了地图校验逻辑：

- 地图宽度最大允许 150
- 地图高度最大允许 150

并优化了自动缩放：

- 100×100 及以上：自动使用 20px 单元格
- 70×70 及以上：自动使用 22px 单元格
- 50×50 及以上：自动使用 24px 单元格
- 35×30 及以上：自动使用 26px 单元格

150×150 地图仍然会很大，需要使用滚动条编辑，但不会再因为宽高限制无法创建。

## 修改文件

- `leveleditordialog.cpp`

## 验收方法

1. 打开 `关卡设计师`。
2. 查看宽度输入框，应该可以调到 `150`。
3. 查看高度输入框，应该可以调到 `150`。
4. 设置 `150 × 150`，点击 `生成地图`。
5. 地图应生成成功，并自动缩小到较小单元格。
6. 使用滚动条查看地图区域。
7. 点击 `校验地图`：
   - 如果没有起点/终点，应正常提示缺少起点或终点；
   - 不应提示宽高超过限制。
8. 放置起点和终点后保存，JSON 应正常生成。
