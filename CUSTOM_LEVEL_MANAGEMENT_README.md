# 自定义关卡管理功能

本次补充了关卡选择界面中的自定义关卡管理能力，目标是让玩家创建后的 JSON 关卡可以直接在游戏内继续维护。

## 新增能力

在“关卡选择”窗口中，自定义关卡会单独显示在“自定义关卡”分组下，每一项包含：

- 试玩：直接进入该自定义关卡。
- 编辑：打开关卡设计师，并加载该 JSON 文件继续编辑。
- 重命名：修改关卡名称，并同步保存为新的 JSON 文件名。
- 删除：删除 custom_levels 文件夹中对应的 JSON 文件。

## 编辑逻辑

点击“编辑”后会进入关卡设计师：

- 会加载关卡名、目标反转次数、地图内容、玩家编辑候选点。
- 保存时默认保存回 custom_levels。
- 如果仍然使用原关卡名，会覆盖更新原 JSON 文件。
- 如果修改了关卡名，会保存成新文件，并尝试删除旧文件，避免列表里出现重复旧版本。
- 设计师中的撤销 / 重做、测试当前关卡、左侧滚动适配、钥匙门、传送门等既有功能继续保留。

## 重命名逻辑

重命名自定义关卡时：

1. 弹出输入框填写新名称。
2. 使用 LevelManager 的安全文件名规则生成 JSON 文件名。
3. 如果目标文件已存在，会询问是否覆盖。
4. 保存新 JSON 成功后，删除旧 JSON 文件。
5. 重新打开关卡选择界面刷新列表。

## 删除逻辑

删除自定义关卡时：

1. 弹出确认框。
2. 只允许删除 custom_levels 文件夹内的文件。
3. 拒绝删除 levels 或其他目录中的文件，避免误删内置关卡或任意本地文件。
4. 删除成功后刷新关卡选择界面。

## 文件路径安全

Level 新增了 sourceFilePath 字段，用于记录关卡 JSON 的来源文件。

LevelManager 删除自定义关卡时会检查：

- 该关卡必须是自定义关卡。
- 文件路径不能为空。
- 文件必须位于 custom_levels 文件夹内。
- 文件必须真实存在。

这样可以避免通过伪造路径删除项目外部文件。

## 主要涉及文件

- level.h / level.cpp：Level 新增 sourceFilePath。
- levelmanager.h / levelmanager.cpp：新增自定义关卡路径、删除、保存辅助能力。
- leveleditordialog.h / leveleditordialog.cpp：支持加载已有自定义关卡并保存回原文件。
- mainwindow.cpp：关卡选择界面增加自定义关卡的试玩、编辑、重命名、删除按钮。
