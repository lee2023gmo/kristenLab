# 上下墙支撑滚动修正版

## 这次修正的核心

你指出的问题是：

> 小球从左往右贴着下面的墙滚动时，如果下面的墙没了，应该向下掉，而不是继续吸在右墙或卡在那里。

本版本修正为：

1. 只有“上方墙”和“下方墙”算作支撑面。
2. 左墙、右墙不再算支撑面。
3. 小球不能吸在左墙或右墙上。
4. 在下方墙上左右滚动时，如果下方墙断开，小球会向下掉。
5. 在上方墙上左右滚动时，如果上方墙断开，小球会向上掉。
6. 四面都没有上/下支撑时，不能通过方向键改变运动方向。

## 操作规则

### 贴着下方墙

允许：

- 按 ←：向左滚
- 按 →：向右滚
- 按 ↑：向上离开墙

不允许：

- 按 ↓：继续往墙里顶

如果向左/右滚动时，下方墙没了：

- 自动向下掉落

### 贴着上方墙

允许：

- 按 ←：向左滚
- 按 →：向右滚
- 按 ↓：向下离开墙

不允许：

- 按 ↑：继续往墙里顶

如果向左/右滚动时，上方墙没了：

- 自动向上掉落

### 只贴着左墙或右墙

不允许靠左右墙改变方向。

这样小球不会吸在左墙或右墙上。

## 修改文件

- `gamescene.cpp`

## 验收方法

### 验收 1：地面平台断开后向下掉

1. 做一段下方平台。
2. 让小球重力向下落到平台上。
3. 按 → 让小球沿着平台向右滚。
4. 当小球滚到平台末端、下面没有墙时，小球应该向下掉。
5. 不应该继续水平飞。
6. 不应该吸在右墙上。

### 验收 2：天花板断开后向上掉

1. 做一段上方墙。
2. 让小球重力向上贴住上方墙。
3. 按 → 让小球沿着上方墙向右滚。
4. 当小球滚到上方墙末端、上面没有墙时，小球应该向上掉。
5. 不应该继续水平飞。
6. 不应该吸在右墙上。

### 验收 3：左右墙不再提供吸附

1. 让小球碰到左墙或右墙。
2. 如果它没有同时贴着上方墙或下方墙，方向键不应该让它沿左右墙滚动。
3. 小球不会挂在左墙或右墙上。

### 验收 4：贴着下方墙时仍可左右滚和向上离开

1. 小球停在下方墙上。
2. 按 ← 或 →，应该可以左右滚。
3. 按 ↑，应该可以向上离开。
4. 按 ↓，不应该继续顶进墙。

### 验收 5：贴着上方墙时仍可左右滚和向下离开

1. 小球贴着上方墙。
2. 按 ← 或 →，应该可以左右滚。
3. 按 ↓，应该可以向下离开。
4. 按 ↑，不应该继续顶进墙。
