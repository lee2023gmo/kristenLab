# 贴墙滚动后墙断开自动坠落 + 设计师地图区域自适应

## 本次修改目标

1. 小球沿着墙滚动时，如果贴着的墙突然断开，小球会自动按照当前重力方向坠落。
2. 关卡设计师中的地图编辑区域会根据地图规模自动变大，减少连续拖动绘制时被小框限制的问题。

## 修改文件

- `gamescene.h`
- `gamescene.cpp`
- `leveleditordialog.h`
- `leveleditordialog.cpp`

## 小球规则修正

之前的问题：

- 小球重力向上时贴着上方墙。
- 按右键/右方向沿着上方墙滚动。
- 如果上方墙走着走着没了，小球可能继续水平移动或卡住。

现在的规则：

- 如果当前重力是 `Up`，但小球不再贴着上方墙，就自动恢复向上坠落。
- 如果当前重力是 `Down`，但小球不再贴着下方墙，就自动恢复向下坠落。
- 如果当前重力是 `Left`，但小球不再贴着左侧墙，就自动恢复向左坠落。
- 如果当前重力是 `Right`，但小球不再贴着右侧墙，就自动恢复向右坠落。

## 设计师窗口优化

现在点击 `生成地图` 或 `导入 JSON` 后：

- 表格区域会根据地图行列数自动变大。
- 地图框也会跟着变大。
- 设计师窗口会适当放大。
- 大地图仍然保留最大尺寸，避免窗口过大。

## 验收方法

### 验收 1：贴着上方墙，墙断开后向上坠落

1. 设计一张地图，上方有一段横向墙，但中间或右侧有缺口。
2. 让小球重力向上，贴到上方墙。
3. 按右方向键，让小球沿着上方墙向右滚。
4. 当小球滚到上方墙结束的位置时，应该自动向上掉落。
5. 不应该卡在空中，也不应该继续只水平移动。

### 验收 2：贴着下方墙，墙断开后向下坠落

1. 设计一段地面平台，右边有缺口。
2. 让小球重力向下，停在平台上。
3. 按右方向键，让小球沿平台滚动。
4. 滚出平台后，应该自动向下坠落。

### 验收 3：撞墙仍然不反弹

1. 让小球朝墙移动。
2. 碰到墙后应该停住。
3. 不应该反弹回来。

### 验收 4：空中仍然不能随便切换方向

1. 让小球四面都不贴墙。
2. 按方向键。
3. 重力方向不应该改变。

### 验收 5：关卡设计师地图框自适应

1. 打开关卡设计师。
2. 设置宽度 12，高度 8，点击生成地图。
3. 地图表格区域应该能完整或更大范围显示。
4. 再设置更大的地图，比如宽度 20，高度 12。
5. 点击生成地图后，窗口和地图框应该变大，连续拖动绘制更方便。
