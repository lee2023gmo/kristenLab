# 钥匙与门功能说明

本次新增两个地图元素：

| 元素 | 字符 | 说明 |
|---|---|---|
| 钥匙 | `K` | 角色进入钥匙所在格子后自动拾取，钥匙格变为空地。 |
| 门 | `A` | 未拾取钥匙时等同墙体，会阻挡角色；拾取至少一把钥匙后，所有门打开并可通行。 |

## 运行规则

1. 当前版本采用“一把钥匙打开全部门”的规则。
2. 钥匙是访问型道具：角色中心进入 `K` 所在格子时自动拾取，不需要额外按键。
3. 钥匙被拾取后，地图运行数据中该格改为 `0`，避免重复拾取。
4. 未拾取钥匙时，`A` 会参与碰撞与支撑判定，行为等同墙体。
5. 拾取钥匙后，`A` 不再阻挡；门的视觉会变淡，表示已经打开。
6. 状态栏会在当前关卡存在钥匙时显示 `钥匙：已拾取/总数`。

## 关卡设计师变化

关卡设计师的元素下拉框新增：

```text
钥匙 K
门 A
```

保存 JSON 时，地图字符串会直接保存 `K` 和 `A`。

示例：

```json
{
  "name": "钥匙门测试",
  "targetReverseCount": 6,
  "map": [
    "1111111",
    "120K0A3",
    "1111111",
    "1111111",
    "1111111"
  ]
}
```

## 校验标准

地图基础校验保持不变：

1. 地图不能为空。
2. 每一行长度必须一致。
3. 编辑器中地图宽度必须在 `5 ~ 150`，高度必须在 `5 ~ 150`。
4. 地图必须有且只有一个起点 `2`。
5. 地图至少需要一个终点 `3`。
6. 地图字符必须是已知元素。
7. 如果地图中存在门 `A`，必须至少存在一把钥匙 `K`，避免门永远无法打开。

新增后合法字符包括：

```text
0 空地
1 墙体
2 起点
3 终点
4 死亡区
5 弹射块（旧关卡兼容）
6 缓冲区
7 传送带（旧关卡兼容）
8 数据碎片
L 激光门
K 钥匙
A 门
9/u/v/w/r/l 蹦床方向
```

玩家编辑候选点校验：

1. 候选点不能越界。
2. 候选点不能重复。
3. 候选点不能放在墙、起点、终点、死亡区、数据碎片、钥匙、门、弹射块、传送带上。
4. 候选点仍然允许放在空地、缓冲区、激光门、蹦床等可编辑机制位置上。

## 修改文件

```text
tiledefs.h
gamescene.h
gamescene.cpp
leveleditordialog.cpp
levelmanager.cpp
```

## 示例关卡

新增了一个示例关卡：

```text
levels/key_door_demo.json
```

用于快速验证钥匙拾取和门打开逻辑。
