# Stage 24 修改说明

本压缩包基于阶段 23 继续修改。

## 已实现

阶段 24：表格式地图生成。

现在在“关卡设计师”窗口中可以：

1. 输入地图宽度
2. 输入地图高度
3. 点击 `生成地图`
4. 生成一个对应大小的 `QTableWidget`
5. 每个格子默认显示 `0`
6. 每个格子当前都可以双击编辑字符
7. 重新修改宽高后，再点 `生成地图` 可以重新生成表格

## 尺寸限制

- 宽度最小：5
- 宽度最大：30
- 高度最小：5
- 高度最大：20

## 验收方式

运行程序后：

1. 主菜单点击 `关卡设计师`
2. 输入宽度：12
3. 输入高度：8
4. 点击 `生成地图`
5. 应该出现 8 行 × 12 列表格
6. 每个格子默认显示 `0`
7. 修改宽度和高度后再次点击 `生成地图`，表格会重新生成

## 注意

阶段 24 只做表格生成。
阶段 25 会继续实现“选择地图元素并点击格子绘制”的功能。
